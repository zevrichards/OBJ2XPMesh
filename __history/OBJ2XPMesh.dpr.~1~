program OBJ2XPMesh;

uses
  Vcl.Forms,
  OBJ2Mesh in 'OBJ2Mesh.pas' {Form1},
  rscommonfunctions in '..\rscommonfunctions.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
